To install the configuration, run **exactly the following commands**

    cd $env  # your clone of this repo
    ./install.sh

You'll be prompted to remove files that are in the way and to create
necessary directories that don't exist.